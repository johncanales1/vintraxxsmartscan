import { DebugAccountDropdown } from "@/components/debug-account-dropdown";

export default function DebugPage() {
    return (
        <div className="min-h-screen bg-gray-50">
            <DebugAccountDropdown />
        </div>
    );
}
